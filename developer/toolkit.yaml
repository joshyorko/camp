tasks:
  robot:
    shell: python call_invoke.py robot
  robotKubernetes:
    shell: python call_invoke.py robotKubernetes
  package:
    shell: python call_invoke.py package

devTasks:
  local:
    shell: python call_invoke.py local
  install:
    shell: python call_invoke.py install
  test:
    shell: python call_invoke.py test
  doesItWorkOnMyMachine:
    shell: python ../robot_tests/resources/DoesItWorkOnMyMachine.py

environmentConfigs:
  - setup.yaml

artifactsDir: ../build/evidence

PATH:
PYTHONPATH:

ignoreFiles:
  - .gitignore
