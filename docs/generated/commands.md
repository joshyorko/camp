<!-- Code generated by go run ./cmd/camp-docs; DO NOT EDIT. -->

# Camp command reference

This reference is generated from the production Cobra tree. A listed command is wired, but its presence is not evidence that external tools or a complete lifecycle succeeded.

## `camp`

Recoverable capsule workspaces

```text
Usage:
  camp

Flags:
      --json   emit stable JSON output
```

## `camp close`

Publish a checkpoint and close

```text
Usage:
  camp close

Global flags:
      --json   emit stable JSON output
```

## `camp completion`

Generate shell completion

```text
Usage:
  camp completion [bash|zsh|fish]

Global flags:
      --json   emit stable JSON output
```

## `camp doctor`

Diagnose required host capabilities

```text
Usage:
  camp doctor

Global flags:
      --json   emit stable JSON output
```

## `camp init`

Initialize a capsule root

```text
Usage:
  camp init [root] [flags]

Flags:
      --backend string           persist the default backend URL
      --capsule string           persist the default capsule name
      --devpod-context string    persist the DevPod context (default "default")
      --devpod-provider string   persist the default DevPod provider
      --source string            persist the default source path

Global flags:
      --json   emit stable JSON output
```

## `camp open`

Open a capsule workspace

```text
Usage:
  camp open [target]

Global flags:
      --json   emit stable JSON output
```

## `camp recover`

Recover an interrupted lifecycle

```text
Usage:
  camp recover [target]

Global flags:
      --json   emit stable JSON output
```

## `camp reopen`

Reopen a closed capsule workspace

```text
Usage:
  camp reopen [target]

Global flags:
      --json   emit stable JSON output
```

## `camp setup`

Install or reuse pinned DevPod and Hauler tools

```text
Usage:
  camp setup

Global flags:
      --json   emit stable JSON output
```

## `camp sync`

Publish a checkpoint and remain open

```text
Usage:
  camp sync

Global flags:
      --json   emit stable JSON output
```

