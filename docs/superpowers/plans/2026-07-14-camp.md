# Camp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the complete Linux lifecycle CLI described by `prompts/camp-codex-build-prompt.md`, using the Oracle-corrected recoverable publication protocol and the direct user override to `skevetter/devpod` v0.26.1.

**Architecture:** Camp is one Go/Cobra binary organized as pure domain invariants, application use cases, ports, and external adapters. Every external side effect is journaled as intent then verified fact; immutable generations, lineage-scoped leases, explicit materialization ownership, a persistent supervisor, and reconciliation states make publication and cleanup independently recoverable.

**Tech Stack:** Go 1.24+, Cobra, YAML v3, AWS SDK for Go v2, klauspost zstd, DevPod v0.26.1, Hauler v2.0.1, Linux amd64/arm64.

## Global Constraints

- The capsule is the complete adopted root; exclude exactly `.camp/build/` and `.camp/runtime/` from the inner archive.
- `.camp/hauler-manifest.yaml` is the packing contract; do not introduce another bundle format.
- DevPod owns providers, SSH, IDEs, credentials, tunnels, forwarding, and workspace lifecycle.
- Hauler owns OCI-layout stores, saved hauls, registry serving, and file serving.
- No background filesystem synchronization; only explicit `sync` and `close` return workspace bytes.
- Never remove an adopted user directory. Remove a Camp-created XDG materialization only after verified publication, DevPod teardown, forwarder/service shutdown, conditional lease release, and ownership revalidation.
- Lock `skevetter/devpod` v0.26.1 at `86b6f9f5d6713fecdeff5dd240e775a8c7e8d44e`; amd64 SHA-256 `01bbc2d88090d546e04aa435c63fc5eb95ec49ffb7ab102a67de0d6d12c82d8d`; arm64 SHA-256 `268621da428ca6d470d1812d63c9e41a1681b681861fc984648a57c5725478ee`.
- Lock `hauler-dev/hauler` v2.0.1 at `4f47155d6f8ccec22ba6f609f2f1f4919b02fce1`; amd64 SHA-256 `c8b65ea57f10a03f02104ebbecbf1249c41f499bc85771488824b70f6ccfc76c`; arm64 SHA-256 `ffe9811bbb77f844200f0644b98a08b65ee2d5ffcc0e2b49b1df31719a0a052f`.
- Lock the Room of Requirement fixture v1.18.0 at `0aabf18ad291c590498bd8e904a7d09f66378b85`.
- Existing root devcontainer configuration wins. Generate the digest-locked Wolfi fallback only under `.camp/runtime/`.
- Preserve every named OCI image and direct tagged registry push. Do not promise capture of dangling layers, opaque BuildKit cache, untagged manifests, digest-only objects, or arbitrary OCI artifacts.
- Every durable schema is versioned. Every secret-bearing file is mode 0600. Stable JSON and human presenters consume the same typed events and operation results.
- Treat ambiguous subprocess, upload, pointer, DevPod, and cleanup outcomes as reconciliation states; never retry destructive operations blindly.

---

### Task 1: Freeze external contracts and safety schemas

**Files:**
- Create: `go.mod`, `cmd/camp/main.go`
- Create: `internal/domain/{capsule,lineage,generation,lease,session,checkpoint,materialization,image}.go`
- Create: `internal/ports/{runner,artifactstore,events}.go`
- Create: `internal/adapters/{devpod,hauler}/client.go`
- Create: `internal/adapters/tools/lock.go`, `tools.lock.yaml`
- Test: colocated `*_test.go` files and `integration/contracts_test.go`

**Interfaces:**
- `runner.Run(ctx, Command) (Result, error)` accepts executable, argv, directory, environment additions, streams, and redaction metadata without shell concatenation.
- `devpod.Client` exposes `Up`, `SSH`, `Status`, `Stop`, `Delete`, `ResolveWorkspaceFolder`, and forwarding argv.
- `hauler.Client` exposes load, extract, sync, add-image, save, info, registry, and fileserver argv with `store --store STORE` ordering.
- All durable documents contain `schemaVersion: 1`; generations contain immutable metadata sidecars.

- [ ] Write failing table tests for exact DevPod terminal argv, status/list JSON states, reverse forwarding, stop/delete gating, and exact Hauler v2.0.1 argv.
- [ ] Write failing JSON/YAML round-trip tests for pointer, generation sidecar, lease, journal snapshot, capsule metadata, lock, and image inventory schemas.
- [ ] Run `go test ./internal/domain ./internal/adapters/devpod ./internal/adapters/hauler ./internal/adapters/tools`; expected result is compile failure because the contracts do not exist.
- [ ] Implement the smallest typed contracts, adapters, tool-lock parser, and Cobra executable required by the tests.
- [ ] Run the focused tests, then run a real installed-binary probe and a real Hauler file → save → load → extract round trip.
- [ ] Run `go test -race ./...`, `go vet ./...`, and commit `feat: freeze Camp external contracts`.

### Task 2: Implement equivalent object-store and coordination semantics

**Files:**
- Create: `internal/ports/objectstore.go`
- Create: `internal/adapters/filebackend/store.go`
- Create: `internal/coordination/{pointers,leases,generations,history}.go`
- Test: colocated tests plus multi-process file-backend fixtures.

**Interfaces:**

```go
type Revision string
type ObjectMeta struct {
    Key string
    Size int64
    Revision Revision
    SHA256 string
    Modified time.Time
}
type WriteCondition struct {
    MustBeAbsent bool
    MatchRevision Revision
}
type ObjectStore interface {
    Get(context.Context, string) (io.ReadCloser, ObjectMeta, error)
    Head(context.Context, string) (ObjectMeta, error)
    PutImmutable(context.Context, string, RestartableSource, string, int64) (ObjectMeta, error)
    PutConditional(context.Context, string, []byte, WriteCondition) (ObjectMeta, error)
    DeleteConditional(context.Context, string, Revision) error
    List(context.Context, string, string) ([]ObjectMeta, string, error)
}
```

- [ ] Write failing tests for immutable equal-byte acceptance, unequal-byte rejection, create-if-absent, stale revision conflict, conditional delete, pagination, crash-safe temporary files, and concurrent writers.
- [ ] Write failing repository tests for main and branch pointer paths, lineage-scoped leases, acquire/revalidate/renew/release, generation sidecars, history listing, and opened/current baseline separation.
- [ ] Run the focused tests and confirm failures arise from missing behavior.
- [ ] Implement per-key interprocess locking, compare-under-lock, same-filesystem temporary writes, file fsync, atomic rename, parent-directory fsync, opaque revisions, and repository rules.
- [ ] Run focused tests, `go test -race ./internal/adapters/filebackend ./internal/coordination`, and commit `feat: add safe file-backed coordination`.

### Task 3: Deliver the complete local file-backend lifecycle vertical

**Files and responsibilities:**

- Create `internal/ports/{journal,process,portallocator,clock,hostidentity,workspace,registry,confinement}.go`: durable recording, supervised-process and service-unit inspection, endpoint, identity, workspace, registry, and typed confinement boundaries.
- Create `internal/adapters/subprocess/runner.go` and `internal/adapters/host/{clock,identity}.go`: structured one-shot execution plus real clock/machine/process identity; never shell-concatenate input.
- Create `internal/journal/{store,operation_lock,reconcile}.go`: mode-0600 intent/fact log, atomic snapshots, PID/start-identity operation lock, and outcome-unknown reconciliation.
- Create `internal/capsule/{source,ownership,initialize,devcontainer}.go`: two-source selection, automatic adoption, ownership marker/device/inode checks, stable `.camp` documents, and digest-locked fallback overlay under `.camp/runtime/`.
- Create `internal/config/{bootstrap,runtime,redact}.go` and `internal/target/resolve.go`: XDG/two-phase precedence, schema-aware redaction, and canonical root-relative target resolution after hydration.
- Create `internal/adapters/archive/tarzstd.go`, `internal/adapters/hauler/{manifest,generation,services}.go`, and `internal/checkpoint/builder.go`: secure whole-root archive, deterministic manifest, fresh-store generation assembly, real-Hauler validation, and the only checkpoint build path.
- Create `internal/adapters/supervisor/{process,ports,confinement,supervisor}.go` and `internal/app/supervise.go`: persistent lease heartbeat; `PastaLoopback` helper-plus-Hauler-child service units; launch-token/pidfile discovery; launch-and-retry port selection; loopback and namespace postconditions; reachability/forwarder liveness; readiness, logs, orphan reconciliation, child-first shutdown, and PID reuse protection; no filesystem watcher or background transfer.
- Create `internal/workspace/{local,reachability}.go`: `LocalNoop` return transport and capability-based in-workspace reachability.
- Create `internal/images/{engine,inventory,references,capture,restore}.go` and `internal/registry/{catalog,snapshot}.go`: Docker-then-Podman named-image inventory, deterministic private references, direct-push catalog merge, digest verification, barriered registry snapshot, idempotent restore, and original-name retagging.
- Create `internal/app/{init,open,sync,close,recover,status,checkpoint}.go`, `internal/cli/{root,init,open,sync,close,recover,status,supervise}.go`, and `internal/presentation/{human,json}.go`: thin Cobra handlers over typed use cases/results, including the hidden supervisor entrypoint.
- Modify `cmd/camp/main.go`, `internal/adapters/devpod/client.go`, and `internal/domain/{session,checkpoint,materialization,image}.go` only where the vertical needs executable wiring, typed environment/forwarding inputs, and durable postconditions.
- Create fixture files `integration/testdata/SecondBrain/{README.md,.hidden}`, `integration/testdata/SecondBrain/MemoryD/{seed.txt,Dockerfile}`. Test `internal/adapters/subprocess/runner_test.go`, `internal/adapters/host/{clock,identity}_test.go`, `internal/journal/{store,operation_lock,reconcile}_test.go`, `internal/capsule/{source,ownership,initialize,devcontainer}_test.go`, `internal/config/{bootstrap,runtime,redact}_test.go`, `internal/target/resolve_test.go`, `internal/adapters/archive/tarzstd_test.go`, `internal/adapters/hauler/{manifest,generation,services}_test.go`, `internal/adapters/supervisor/{process,ports,supervisor}_test.go`, `internal/workspace/{local,reachability}_test.go`, `internal/images/{engine,inventory,references,capture,restore}_test.go`, `internal/registry/{catalog,snapshot}_test.go`, `internal/checkpoint/builder_test.go`, `internal/app/{init,open,sync,close,recover,status,checkpoint}_test.go`, `internal/cli/lifecycle_test.go`, `internal/presentation/presentation_test.go`, `integration/local_vertical_test.go`, and `integration/local_crash_recovery_test.go`.

**Interfaces and ordering constraints:**

- Consume the Task 1-2 `ports.ObjectStore`, `coordination.{PointerRepository,LeaseRepository,GenerationRepository}`, `PointerBaseline`, durable domain records, and exact DevPod/Hauler clients; do not duplicate coordination policy.
- `Journal.RecordIntent` must fsync before each external effect; `Journal.RecordFact` must record only an observed postcondition. Unpaired intent is explicit reconciliation work, never permission to blindly repeat DevPod, upload, pointer, process-stop, or cleanup effects.
- `CheckpointPublisher` is the only path allowed to mirror/no-op, snapshot, build, real-Hauler validate, upload, remote-readback verify, pointer-CAS publish, and atomically advance the current baseline while retaining the opened generation.
- Open order is `SessionCreated` -> `BootstrapConfigResolved` -> `ConfinementResolved` -> `SourceResolved` -> `PointerObserved` -> `LeaseAcquired|ReadOnlySelected` -> `PointerRevalidated` -> fetch/verify/load/stage/commit or `LocalRootAdopted` -> `CapsuleConfigApplied` -> `TargetResolved` -> `EndpointCandidatesRecorded` -> `ServicesReady` -> `EndpointsCommitted` -> `DevPodUpReconciled` -> `WorkspaceRootResolved` -> `ForwardingReady` -> `ImagesRestored` -> `SessionReady` -> `EntryDispatched`.
- Sync order is `CheckpointStarted` -> `LeaseValidated` -> `WorkspaceMirrored(LocalNoop)` -> `RootSnapshotStable` -> image inventory/push -> barriered `RegistrySnapshotSealed` -> atomic inventory/manifest commit -> inner build/verify -> fresh store/save/real-Hauler validate -> immutable upload/remote verify -> pointer CAS -> baseline advance -> immutable serving refresh -> `CheckpointComplete`.
- Close keeps one local operation lock and calls the same publisher: `CloseStarted` -> final checkpoint or read-only discard -> reconciled DevPod delete/stop -> forwarders stopped -> services stopped -> exact lease release -> ownership-revalidated materialization removal -> `SessionClosed`. Publication success and cleanup success remain separate permanent facts.
- Resolve an absolute capability-compatible `pasta` before lease, adoption/materialization, endpoint publication, supervisor detach, or Hauler start. Each registry/fileserver is one supervised `PastaLoopback` unit using foreground/quiet mode, private `--log-file` and `--pid` paths, mandatory `--host-lo-to-ns-lo`, exactly `127.0.0.1/HOST:GUEST`, disabled UDP and namespace-to-host forwarding, a dedicated session/process group, and the exact structured Hauler child command. Raw Hauler service launch and unsafe fallback are prohibited.
- Journal a stable launch token, desired mapping, launcher/version, environment fingerprint, and private pid/log paths before spawn. Readiness then records and validates exact helper and child PID/boot/start identities, exactly one direct child, executable/argv, shared dedicated PGID/SID, a child network namespace distinct from the supervisor, pre-bind absence, exact host IPv4 loopback with no IPv6/wildcard exposure or host listener at `GUEST`, the Hauler-owned guest listener, causal agreement with the recorded mapping, and service-specific HTTP 200 through it. Host `ss` owner text is optional evidence, not a readiness predicate.
- Reconcile pending starts by launch token/pidfile/exact argv and never duplicate a partial unit. Helper loss requires exact orphan-child cleanup; child loss requires helper cleanup; unknown stable-port occupants fail closed. Stop the exact child first, then the exact helper if necessary, and prove both identities, group, namespace, mapping, and owned references absent. Never signal a group without validating every member, and never change an endpoint after DevPod environment injection. Later `pasta` disappearance or incompatibility blocks restart but never blocks cleanup of already-recorded live identities.
- Observe pointer, acquire lineage lease against it, then revalidate before fetch. Resolve landing target only after root commit; map it only after querying the effective workspace root. Record endpoint candidates before service launch, commit only the mapping that passed all readiness postconditions before DevPod environment construction, and prove reachability from the workspace while host listeners remain loopback-only.
- Capture images, atomically write `.camp/images.json`, regenerate deterministic multi-document `.camp/hauler-manifest.yaml`, then build `.camp/build/<capsule>.tar.zst`; exclude exactly `.camp/build/` and `.camp/runtime/`. Reject unstable reads and every required traversal/symlink/hardlink/special-file attack.
- Preserve the mutable registry overlay across generation snapshots. Content before the barrier enters the current generation; content after it remains for the next. Only host-daemon sources use `local: true`.
- Read-only sessions never lease or publish, including recovery. Branch creation is create-if-absent with its source generation as parent and its own pointer/lease/CAS baseline. `CAMP_CHECKPOINT` means the opened checkpoint; current publication state is durable and supplied to later attach operations.
- Inject `CAMP_REGISTRY`, `CAMP_FILESERVER`, `CAMP_CAPSULE`, and opened-value `CAMP_CHECKPOINT` through typed DevPod workspace environment flags. Use a deterministic workspace ID; default close reconciles DevPod deletion and `--keep-workspace` reconciles stop.

**TDD steps:**

- [ ] Write failing journal/supervisor/ownership tests for mode 0600, intent-without-fact, fsync/rename crashes, PID reuse, stale operation locks, heartbeat loss, adopted-root nondeletion, marker/identity mismatch, signal handling, cleanup-only recovery, missing/incompatible `pasta`, corrected exact argv, helper/child loss, crash-before-identity-fact discovery, namespace drift, wildcard/IPv6 exposure, unknown port ownership, partial-unit cleanup, and child-first shutdown.
- [ ] Write failing archive/manifest/registry/image tests for whole-root metadata continuity, exact exclusions, every Oracle archive attack including overlong names, mutation detection, deterministic build order, fresh stores, truthful `local`, multiple tags/platform/digests, catalog pagination, direct pushes, opt-out labels, reference collisions, barrier cuts, digest mismatch, and idempotent restore. With real Hauler, prove a served-registry push is absent before snapshot ingestion and present after save/load.
- [ ] Write failing config/source/target/use-case tests for full precedence/redaction and a credential scan of the haul; explicit adoption versus configured remote; unexplained/symlinked roots; absolute, relative, basename, ambiguity, zoxide in/outside-root, and post-hydration targets; existing devcontainers versus digest-locked `.camp/runtime` fallback; endpoint launch/retry/reuse and workspace reachability; read-only/branch modes; default delete/`--keep-workspace`; status addresses; 42->43->44->45 advancement; ambiguous outcomes; publication/cleanup separation; stable JSON/human failures; and exact recovery commands.
- [ ] Run the focused packages and confirm RED comes from missing behavior, not a broken harness.
- [ ] Implement the smallest behavior that passes each focused group, wiring the application state machine only after its journal, archive, service, registry, and ownership invariants pass.
- [ ] Run the real local exit gate and the crash matrix; a skipped real DevPod, Hauler, engine, registry, publication, cleanup, or reopen assertion is not a passing gate.

**Real exit gate:** Copy `integration/testdata/SecondBrain` to an uninitialized fixture, adopt it with `camp open`, enter by real DevPod terminal mode with explicit current-host selection `--context default --provider docker` and `--ide none --open-ide=false`, land in its `MemoryD` child, edit bytes, build/tag a nested image, `camp sync`, edit again, and `camp close`. The context/provider flags select this grounded integration environment; they do not define Camp product defaults. Prove both host services listen only on their recorded `127.0.0.1` mappings, their Hauler children remain in distinct namespaces, helper loss is reconciled without duplicate or escaped listeners, and all partial units are reaped exactly. Prove the adopted source still exists and only Camp-owned session material is removed. From a clean XDG home, reopen the verified Hauler generation, verify both edits and the restored/runnable image, then close and prove the Camp-created hydrated root is removed only after all cleanup preconditions. Repeat deterministic intent/effect/fact cuts at every transition and run the installed-tool crash subset at every irreversible or destructive boundary, including helper spawn, child spawn, bind, readiness-before-fact, upload-before-CAS, CAS-response loss, pointer-before-baseline, child termination, helper loss, orphan cleanup, and pointer-before-cleanup.

**Verification commands:**

```text
rtk go test ./internal/journal ./internal/capsule ./internal/config ./internal/target ./internal/adapters/archive ./internal/adapters/hauler ./internal/adapters/supervisor ./internal/workspace ./internal/images ./internal/registry ./internal/checkpoint ./internal/app ./internal/cli ./internal/presentation
rtk go test -v ./integration -run '^TestLocalLifecycleVertical$' -count=1 -timeout=60m
rtk go test -v ./integration -run '^TestLocalLifecycleCrashMatrix$' -count=1 -timeout=120m
rtk go test -race ./...
rtk go vet ./...
rtk git diff --check
```

**Commit subject:** `feat: deliver local Camp lifecycle`

### Task 4: Complete the local command, IDE, and operational surface

**Files and responsibilities:**

- Create `internal/app/{attach,list,history,serve,images,config,provider,devpod,hauler}.go` and `internal/cli/{attach,list,history,serve,images,config,provider,devpod,hauler,completion}.go`; extend `internal/app/recover.go` for checkpoint recovery and `internal/cli/{open,recover}.go` for the full public flag contract.
- Create `internal/adapters/devpod/ide.go`, `internal/target/map.go`, `internal/session/select.go`, and `internal/config/update.go`: VS Code-family nested URI adapter, container target mapping, deterministic session selection, and explicit Wolfi digest-lock update.
- Modify `internal/adapters/devpod/client.go` for every typed/repeated `up` and SSH option plus conflict-safe passthrough; all raw delegation still uses the locked executable and structured redaction.
- Test `internal/adapters/devpod/{client,ide}_test.go`, `internal/target/map_test.go`, `internal/session/select_test.go`, `internal/config/update_test.go`, `internal/app/commands_test.go`, `internal/cli/{commands,help,completion}_test.go`, `integration/local_commands_test.go`, and `integration/vscode_insiders_test.go`.

**Interfaces and ordering constraints:**

- All commands are application use cases over the Task 3 session model and emit the same typed events/results to human, verbose, and versioned JSON presenters.
- `attach` resolves a root-relative target, queries the effective workspace folder, then uses DevPod SSH/tmux or IDE reopening. A nested VS Code-family target uses the exact percent-encoded `vscode-remote://ssh-remote+<id>.devpod/<container-target>` URI; Insiders launches `code-insiders`; no redundant interactive SSH shell starts.
- Preserve every directly exposed/repeated DevPod flag, `--insiders`, `--devpod-arg`, and `--`; reject typed/raw conflicts. Forward SSH local/reverse ports, user, environment, agent, GPG, and terminal behavior exactly.
- Deliver `attach`, `list`, `history`, full `recover`, `serve status|logs|restart`, `images list|capture|restore`, `config show --effective --redact`, Wolfi lock update, `provider`, locked `devpod --`/`hauler --`, completions, stable help, and zero/one/multiple-session ambiguity rules.

**TDD steps:**

- [ ] Write failing exact-argv/URI tests for tmux present/absent, root and nested targets, VS Code/Insiders, every public/repeated DevPod and SSH flag, raw passthrough, aliases, conflicts, stop/delete, and no redundant shell.
- [ ] Write failing command tests for active/stopped/dead/PID-reused sessions, main/branch/orphan history, session/checkpoint recovery, cleanup-only recovery, serve restart, images operations, config provenance/redaction, provider/tool delegation, exact recovery errors, help, and bash/zsh/fish completions.
- [ ] Run the first verification command and confirm RED comes from the missing command behavior; implement thin handlers/use cases; rerun it GREEN; then execute every local-provider command through a golden or integration test.

**Real exit gate:** Every documented local-provider command succeeds against the proven Task 3 session; fake `code-insiders` retains real DevPod setup and records the exact nested remote URI with no interactive SSH invocation.

**Verification commands:**

```text
rtk go test ./internal/adapters/devpod ./internal/target ./internal/session ./internal/config ./internal/app ./internal/cli
rtk go test -v ./integration -run 'TestLocalCommandSurface|TestVSCodeInsidersNestedTarget' -count=1 -timeout=45m
rtk go test -race ./...
rtk go vet ./...
rtk git diff --check
```

**Commit subject:** `feat: complete local Camp commands`

### Task 5: Add the remote-provider lifecycle without changing checkpoint semantics

**Files and responsibilities:**

- Create `internal/adapters/sshtransfer/{rsync,tarpipe,forward}.go` and tests: DevPod-hosted transfer commands/pipeline, reverse forwards, listener constraints, and reconciliation.
- Create `internal/workspace/remote.go`; modify `internal/workspace/reachability.go`, `internal/adapters/supervisor/supervisor.go`, and `internal/app/{open,attach,sync,close,recover}.go` only to select the remote capability behind the same Task 3 state machine/publisher.
- Test `internal/adapters/sshtransfer/{rsync,tarpipe,forward}_test.go`, `internal/workspace/remote_test.go`, and `integration/remote_vertical_test.go`.

**Interfaces and ordering constraints:**

- `WorkspaceTransport` remains `LocalNoop` or `DevPodSSHMirror`; the remote implementation uses `<workspace-id>.devpod`, queries the effective root, mirrors deletes/modes/hardlinks/symlinks/dotfiles, excludes exactly `.camp/build/` and `.camp/runtime/`, and validates/regenerates inventory/manifest after return.
- Use a structured `ssh | tar` process pipeline when rsync is unavailable. Never implement SSH or use DevPod export/import as checkpoint transport.
- Supervise loopback-only reverse forwards and test registry/fileserver reachability from the workspace. Remote engine images travel through the tunneled registry and then use the unchanged publisher, CAS, recovery, and cleanup protocol.

**TDD steps:**

- [ ] Write failing tests for generated SSH host, nonstandard effective root, rsync preservation/deletes/exclusions, tar-pipeline fallback, partial mirror discard, killed-forward recovery, local/remote reachability, remote image capture, attach/sync/close/recover, and an assertion that export/import is never invoked.
- [ ] Run the first verification command and confirm RED comes from the missing remote behavior; implement the adapters behind existing ports; rerun it GREEN; then run the complete remote exit gate.

**Real exit gate:** The complete lifecycle passes against an SSH-based remote DevPod fixture with a nonstandard workspace folder, edit/delete/chmod/symlink/hardlink continuity, a named image, disabled-rsync fallback, killed-tunnel reconciliation, and recorded proof of no DevPod export/import call.

**Verification commands:**

```text
rtk go test ./internal/adapters/sshtransfer ./internal/workspace ./internal/app
rtk go test -v ./integration -run '^TestRemoteLifecycleVertical$' -count=1 -timeout=60m
rtk go test -race ./...
rtk go vet ./...
rtk git diff --check
```

**Commit subject:** `feat: add remote DevPod lifecycle`

### Task 6: Add S3/MinIO and multi-writer backend hardening

**Files and responsibilities:**

- Create `internal/adapters/s3backend/{store,multipart,probe}.go` and matching tests; modify `internal/config/{bootstrap,runtime}.go` for endpoint, region, bucket, prefix, path-style, TLS, and default credential-chain configuration without capsule credentials.
- Create `integration/{minio_vertical,s3_multiwriter,mounted_filebackend}_test.go`; reuse the Task 2 coordination repositories and Tasks 3-5 lifecycle unchanged.

**Interfaces and ordering constraints:**

- Implement the existing `ports.ObjectStore` with real conditional create/replace/delete, paginated listing, immutable multipart uploads, abort/resume rules, and trustworthy SHA-256/size metadata or streamed readback. Never treat multipart ETag as SHA-256.
- `probe.go` must reject writer use when conditional mutations or remote verification are unsafe; Task 7 `doctor` consumes the same probe.
- Independent writers retain the losing immutable generation after pointer conflict and can create a branch from the recorded parent. Mounted `file://` behavior retains Task 2 guarantees; no weaker backend-specific coordination path is allowed.

**TDD steps:**

- [ ] Write failing tests for every config field, credential-chain/redaction behavior, conditional create/replace/delete, stale revisions, pagination, TLS/path-style endpoints, interrupted multipart abort, expected checksum/size, readback fallback, unsafe-writer rejection, and conditional lease deletion.
- [ ] Write failing process-level tests where two writers race from one parent, exactly one CAS wins, the loser remains recoverable, and the loser publishes to a branch.
- [ ] Run the first verification command and confirm RED comes from the missing backend behavior; implement AWS SDK for Go v2 behavior; rerun it GREEN; then run the MinIO and mounted-file exit gates.

**Real exit gate:** MinIO passes the complete lifecycle and multipart interruption recovery. Two independent Camp processes race from one parent with one pointer winner and a branch-recoverable loser. The mounted `file://` integration proves conditional pointer/lease operations and verified cleanup.

**Verification commands:**

```text
rtk go test ./internal/adapters/s3backend ./internal/config
rtk go test -v ./integration -run 'TestMinIOLifecycleVertical|TestS3TwoWriterConflict|TestMountedFileBackend' -count=1 -timeout=60m
rtk go test -race ./...
rtk go vet ./...
rtk git diff --check
```

**Commit subject:** `feat: add S3-compatible Camp storage`

### Task 7: Finish managed tools, doctor, packaging, documentation, and release proof

**Files and responsibilities:**

- Create `internal/adapters/tools/install.go`, `internal/app/{setup,doctor}.go`, `internal/cli/{setup,doctor}.go`, and `internal/doctor/doctor.go`; test exact DevPod/Hauler PATH acceptance, bootstrap/install concurrency, external-host-capability resolution, and every capability probe.
- Create `README.md`, `docs/{architecture,troubleshooting,backends,install,release,command-reference}.md`, `CONTRIBUTING.md`, and `docs/reviews/2026-07-14-final-requirements-audit.md`; update `docs/adr/*.md` only where final evidence exposes drift.
- Create `.devcontainer/devcontainer.json`, `Makefile`, `.goreleaser.yaml`, `packaging/homebrew/camp.rb`, and `.github/workflows/{ci,release}.yml`.
- Test `internal/adapters/tools/install_test.go`, `internal/doctor/doctor_test.go`, `internal/app/{setup,doctor}_test.go`, `internal/cli/{setup,doctor}_test.go`, `integration/{install_smoke,docs_commands,room_matrix,rust_continuity,registry_cache_export,release}_test.go`.

**Interfaces and ordering constraints:**

- Accept compatible locked DevPod and Hauler binaries already on PATH; otherwise atomically install their checksum-verified Linux amd64/arm64 assets. DevPod stays `skevetter/devpod` v0.26.1 at `86b6f9f5d6713fecdeff5dd240e775a8c7e8d44e` with the committed hashes; Hauler stays v2.0.1 at `4f47155d6f8ccec22ba6f609f2f1f4919b02fce1`. No curl-pipe-shell path. Classify `pasta` separately as an `external-host-capability`: validate it but never add it to `tools.lock.yaml`, download it directly, invoke a host package manager, or pretend version equality proves runtime compatibility.
- Native RPM/DEB/APK packages depend on `passt`, the Homebrew formula declares `depends_on "passt"`, and generic archives document `passt` as an external prerequisite. Direct Camp-on-Wolfi remains unsupported until an audited host package exists; a Wolfi workspace guest does not need the host confinement helper.
- `doctor` probes Linux/architecture, backend conditional/verification semantics, exact DevPod/Hauler binaries, SSH, rsync/tar, optional tmux/zoxide, engine/provider, ports, credentials, workspace reachability, and Room image availability; connectivity alone is never “healthy.” Its `pasta` probe reports path/version and required option surface, user/network namespace policy, `/dev/net/tun`, host/container boundary, then launches a Camp-owned child through one explicit loopback translation and proves namespace separation, exact host listener scope, readiness, identity-safe cleanup, and typed failure causes.
- README follows the required 12-part product structure and exact slogan, includes the command tree generated from the binary, file/Longhorn and MinIO examples, security/recovery guarantees, honest cache/image limits, one compact architecture diagram, and tested transcripts.
- Release produces archives/packages, Homebrew metadata, checksums, SBOM, bash/zsh/fish completions, vulnerability/race/integration jobs, clean install smoke, and explicit credential-gated provider jobs.

**TDD steps:**

- [ ] Write failing tests for exact-version/fork PATH acceptance, rejection, checksum failure, interrupted/concurrent bootstrap, atomic install, external `pasta` discovery and functional-probe failures, setup/doctor capabilities, generated references/transcripts, completions, package dependencies, Homebrew, SBOM/checksums, and release smoke.
- [ ] Run the focused tool/doctor/package/doc tests and confirm RED comes from missing behavior; implement tools, doctor, packaging, workflows, and docs; rerun them GREEN; execute every documented command rather than snapshotting prose alone.
- [ ] Run all published Room variants: `wolfi`, `secure`, `latest`, `ubuntu-noble`, `ubuntu-noble-dind`, `codespaces`, and `debian-trixie`. Wolfi must edit source, run over DevPod SSH, build/tag nested Docker, close/reopen clean, restore/run the image, and verify the file. Prove Rust `target` continuity and an explicit registry cache export; never claim dangling layers or opaque daemon cache.
- [ ] Audit every build-prompt clause and every Oracle finding/P0/P1/P2 gate to fresh file/test evidence; record credential-gated tests exactly and leave no unchecked requirement.

**Real exit gate:** On clean supported Linux fixtures, install each archive/package and run the Homebrew-tap path `brew install camp`; let Camp provision exact DevPod/Hauler binaries and validate the packaged/external `pasta` capability; complete file and MinIO open-sync-close-reopen cycles; use terminal and VS Code Insiders nested targets; restore named images; remove only owned materialization; and recover every injected crash state. Every credential-free release job passes; credential-only provider jobs are present and clearly gated.

**Verification commands:**

```text
rtk go test -race ./...
rtk go vet ./...
rtk go test -v ./integration -run 'TestInstallSmoke|TestDocsCommands|TestRoomMatrix|TestRustContinuity|TestRegistryCacheExport|TestRelease' -count=1 -timeout=120m
rtk govulncheck ./...
rtk make release-check
rtk git diff --check
```

**Commit subject:** `docs: finish Camp release evidence`
